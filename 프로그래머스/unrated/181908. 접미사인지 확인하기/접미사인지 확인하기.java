class Solution {
    public int solution(String my_string, String is_suffix) {
        int answer = 0;
        if(my_string.endsWith(is_suffix)) {
            return 1;
        } else
        return answer;
    }
}

// endsWith() 함수는 대상 문자열이 특정 문자 또는 문자열로 끝나는지 체크하는 함수이다.
// - 해당 문자열로 끝나는지 여부를 확인하고 boolean에 맞춰 true/false 값을 리턴한다.

// 추가) startsWith() 함수는 대상 문자열이 특정 문자 또는 문자열로 시작하는지 체크하는 함수이다.
// - 해당 문자열로 시작하는지 여부를 확인하고 boolean에 맞춰 true/false 값을 리턴한다.
